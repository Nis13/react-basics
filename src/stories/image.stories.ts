import { Meta } from "@storybook/react";
import Image from "../components/image";

const meta = {
  title: "image",
  component: Image,
  tags: ["autodocs"],
} satisfies Meta<typeof Image>;

export default meta;
